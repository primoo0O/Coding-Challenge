JavaScript
- JavaScript is used to build interactive website
- JavaScript can run through browser because they have js engine and node

Primitive/ Value Types
- strings, number, boolean, null and undefined

Reference Types
- Object, Array, and Function
Object - in programming is like a person they have different personality

Static and Dynamic Languages
- Statis means that if you assign an value type to the variable it cannot be change wehn you re assign
- Dynamic can be can be even when ou assign new variable

var 

- is a function scope means if declare within a function it can be only be accessed through that function.
- it can be redeclare and updated within the same scope.

let
- is a block scope where it cannot be called outside of it block
- it can be updated but not redeclare

const 
- const is somewhat similar to let as it cannot be accessed to the different block
and can be accessed to the same block
- Const cannot be updated or redeclare within its scope as it was constant
- is to declare a value that cannot be changed or simply be constant

'' and ""
- Used to create strings
- without embedded variable 

``
- is a back ticks they are called template literals
- allows you to create multi line strings
- with embedded variables or with place variables


